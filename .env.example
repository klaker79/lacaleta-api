# ═══════════════════════════════════════════════════
# lacaleta-api — Variables de entorno
# ═══════════════════════════════════════════════════
# Copia este archivo como .env y rellena los valores.
# NUNCA subas .env a git.
# ═══════════════════════════════════════════════════

# Entorno: development | production
NODE_ENV=development
PORT=3001

# ── Base de datos PostgreSQL ──
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lacaleta102
DB_USER=tu_usuario
DB_PASSWORD=tu_password

# ── Autenticación ──
JWT_SECRET=genera-una-clave-segura-aqui

# ── Sentry (monitoring, opcional en dev) ──
SENTRY_DSN=

# ── Claude AI (parsing PDFs, opcional) ──
ANTHROPIC_API_KEY=

# ── Email (Resend, para verificación y alertas) ──
RESEND_API_KEY=
RESEND_FROM=noreply@tudominio.com

# ── Frontend URL (para CORS y links en emails) ──
FRONTEND_URL=http://localhost:3000

# ── n8n (chatbot IA, opcional) ──
N8N_WEBHOOK_URL=
